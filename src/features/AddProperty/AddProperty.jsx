import { Box } from "@mantine/core";
import AppMap from "../Map/AppMap";

function AddProperty() {
  return (
    <Box w="68vw" h={600} mx="auto">
   < AppMap add={true} />
   </Box>
  )
}

export default AddProperty